# Travian Farm Assistant
Scheduler companion for Travian Farm List.
Server, username, interval and duration are dynamic. Password is not stored by the app.
Default: 15 minutes for 24 hours.
The app sends reminders and opens the configured server; Send All remains manual.
Build: upload this project to GitHub, open Actions, run Build Android APK, then download the artifact.
